#include<stdio.h>
#include<string.h>
int main()
{
    char s[20]="subash";
    char p[20]="subash";
    if(strcmp(s,p)==0)
    {
        printf("it is same");
    }
    else
    { 
        printf("it is not same");
    }
}
